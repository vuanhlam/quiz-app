import { Heading, Paragraph, Button } from '~/components';

function App() {
    return (
        <div style={{ padding: '10px 32px' }}>
            <Button />
            <Button secondery/>
            <Button success/>
            <Button warning/>
        </div>
    );
}

export default App;
